<div class="mt-4">
<h1>Khóa học của tôi</h1>
<p>Bạn đang học <b>{{courses.length}}</b> khóa học tại F8.</p>
    <div class="row">
        {{#each courses}}
        <div class="col-sm-6 col-lg-4">
            <div class="card card-courses-item">
                <a href="/courses/{{this.slug}}">
                    <img src="{{this.image}}" class="card-img-top" alt="{{this.name}}">
                </a>
                <div class="card-body">
                    <a href="/courses/{{this.slug}}">
                        <h5 class="card-title">{{this.name}}</h5>
                    </a>
                    <p class="card-text">{{this.description}}</p>
                    <div class="edit-remove-course">
                    <a href="/courses/{{this._id}}/edit" class="btn btn-warning">Sửa Khóa Học</a>
                    <a href="#" class="btn btn-danger">Xóa Khóa Học</a>
                    </div>
                </div>
            </div>
        </div>
        {{/each}}
    </div>
</div>